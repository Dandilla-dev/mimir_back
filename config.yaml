# Каа — настройки
# Секреты (API ключи) хранятся отдельно в .env

claude:
  model: claude-sonnet-4-6
  max_tokens: 2048
  temperature: 0.7

whisper:
  model: small
  language: auto

voice:
  provider: pyttsx3

server:
  host: 0.0.0.0
  port: 8000
  debug: true
